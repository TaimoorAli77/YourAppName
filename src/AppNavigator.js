import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const AppNavigator = () => {
  return (
    <View>
      <Text> Drawer AppNavigator</Text>
    </View>
  )
}

export default AppNavigator

const styles = StyleSheet.create({})